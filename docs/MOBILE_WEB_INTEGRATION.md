# ChainPass 移动端与Web端协同设计

## 已实现的协同功能

### 1. 扫码登录 ✅

**实现文件:**
- 后端: `QRController.java`, `QRLoginService.java`
- Web: `QRLogin.vue`, `qr.ts`
- 移动端: `qr_scanner_page.dart`, `qr_confirm_page.dart`

**使用流程:**
1. Web端调用 `/qr/create` 创建登录二维码
2. 移动端扫描二维码，调用 `/qr/scan/{sessionId}`
3. 移动端确认登录，调用 `/qr/confirm/{sessionId}`
4. Web端轮询 `/qr/status/{sessionId}` 获取登录结果

### 2. 深度链接 ✅

**实现文件:**
- 移动端: `deep_link_service.dart`

**支持的链接:**
- `chainpass://auth/confirm?sessionId=xxx` - 登录确认
- `chainpass://did/detail?did=xxx` - DID详情
- `chainpass://vc/detail?vcId=xxx` - 凭证详情
- `chainpass://payment/confirm?orderNo=xxx` - 支付确认

### 3. 共享类型模型 ✅

**实现文件:**
- Web: `@chainpass/shared/types`
- 移动端: `shared/models/models.dart`

---

## 当前架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户终端                              │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │  Vue 3 Web  │    │ Flutter App │    │   浏览器     │     │
│  │  (PC/移动)   │    │ (iOS/Android)│    │  (扫码等)   │     │
│  └──────┬──────┘    └──────┬──────┘    └──────┬──────┘     │
└─────────┼──────────────────┼──────────────────┼─────────────┘
          │                  │                  │
          ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────────────┐
│                     统一后端 API (Spring Boot)               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 认证服务  │ │ DID服务  │ │ 支付服务  │ │ KYC服务  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
└─────────────────────────────────────────────────────────────┘
```

## 协同场景设计

### 1. 登录协同

```
┌────────────┐                    ┌────────────┐
│   Web端    │                    │  移动端    │
└─────┬──────┘                    └─────┬──────┘
      │                                 │
      │  1. 显示登录二维码               │
      │  (包含 sessionId)               │
      │                                 │
      │         2. 扫描二维码            │
      │◄────────────────────────────────│
      │                                 │
      │  3. 移动端确认登录               │
      │     (已登录设备扫码确认)          │
      │                                 │
      │  4. Web端自动登录                │
      │     (WebSocket推送)             │
      │                                 │
```

### 2. 身份认证协同

```
场景：Web端进行敏感操作需要移动端确认

┌────────────┐                    ┌────────────┐
│   Web端    │                    │  移动端    │
└─────┬──────┘                    └─────┬──────┘
      │                                 │
      │  1. 发起敏感操作                 │
      │     (大额转账、DID吊销等)         │
      │                                 │
      │  2. 显示确认二维码               │
      │                                 │
      │         3. 扫码确认              │
      │◄────────────────────────────────│
      │                                 │
      │  4. 移动端验证身份               │
      │     (生物识别/PIN)              │
      │                                 │
      │  5. 推送确认结果                 │
      │◄────────────────────────────────│
      │                                 │
```

### 3. DID/VC 展示协同

```
场景：线下验证身份

┌────────────┐                    ┌────────────┐
│  移动端    │                    │  验证方    │
│ (持证者)   │                    │ (Web/App) │
└─────┬──────┘                    └─────┬──────┘
      │                                 │
      │  1. 展示VC二维码                │
      │     (包含VP)                   │
      │                                 │
      │         2. 扫描验证             │
      │────────────────────────────────►│
      │                                 │
      │  3. 验证结果                    │
      │◄────────────────────────────────│
      │                                 │
```

### 4. 支付协同

```
场景：Web端发起支付，移动端确认

┌────────────┐    ┌────────────┐    ┌────────────┐
│   Web端    │    │   后端     │    │  移动端    │
└─────┬──────┘    └─────┬──────┘    └─────┬──────┘
      │                 │                 │
      │  1. 创建支付订单 │                 │
      │────────────────►│                 │
      │                 │                 │
      │  2. 返回订单号   │                 │
      │◄────────────────│                 │
      │                 │                 │
      │                 │  3. 推送通知    │
      │                 │────────────────►│
      │                 │                 │
      │                 │  4. 用户确认    │
      │                 │◄────────────────│
      │                 │                 │
      │  5. 支付完成通知 │                 │
      │◄────────────────│                 │
      │                 │                 │
```

## 技术实现方案

### 1. 深度链接 (Deep Link)

- **URL Scheme**: `chainpass://`
- **Universal Links (iOS)**: `https://app.chainpass.io`
- **App Links (Android)**: `https://app.chainpass.io`

支持的深度链接：
- `chainpass://auth/confirm?sessionId=xxx` - 登录确认
- `chainpass://did/detail?did=xxx` - DID详情
- `chainpass://vc/detail?vcId=xxx` - 凭证详情
- `chainpass://payment/confirm?orderNo=xxx` - 支付确认

### 2. 二维码标准

登录/操作确认二维码格式：
```json
{
  "type": "login_confirm",
  "sessionId": "xxx",
  "timestamp": 1234567890,
  "expiresIn": 300,
  "callback": "https://api.chainpass.io/auth/qr-confirm"
}
```

### 3. 数据同步机制

- **实时同步**: WebSocket 长连接
- **离线支持**: 本地缓存 + 增量同步
- **冲突解决**: 最后写入胜出 (LWW)

### 4. 推送通知

- **FCM (Firebase Cloud Messaging)**: Android
- **APNs (Apple Push Notification)**: iOS
- **Web Push**: Web端

### 5. 会话共享

- **Token同步**: 通过二维码扫描或账号登录
- **多设备管理**: 设备列表、踢出设备
- **安全策略**: 敏感操作需主设备确认