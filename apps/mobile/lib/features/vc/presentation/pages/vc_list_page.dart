import 'package:flutter/material.dart';

/// VC 凭证列表页面
class VCListPage extends StatelessWidget {
  const VCListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('可验证凭证')),
      body: const Center(child: Text('凭证列表页面 - 待实现')),
    );
  }
}